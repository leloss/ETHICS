# Core dependencies used by scripts/ — pinned for reproducible evidence.
# Verified to install on Python 3.11 (CI) through 3.13.
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
fairlearn==0.10.0
PyYAML==6.0.2

# Dev tooling
pre-commit==3.8.0

# Optional ecosystem tools are in requirements-optional.txt. They are not imported
# by any script here, so the core install stays small and quick to reproduce.
